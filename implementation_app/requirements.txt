# Core dependencies for Raspberry Pi 5 LRFR Application
# Optimized for Ubuntu 24.04 on ARM64

# Deep Learning (CPU-optimized for ARM)
torch==2.2.0
torchvision==0.17.0

# Computer Vision
opencv-python-headless==4.9.0.80  # Headless for lower memory footprint
Pillow==10.2.0


# Face Detection (lightweight)
# Note: We'll use OpenCV's Haar Cascades (built-in, no extra dependency)

# Utilities
numpy==1.26.4
tqdm==4.66.1

# Configuration and Data Management
PyYAML==6.0.1

# Performance Monitoring (optional, for debugging)
psutil==5.9.8

# Image Processing
scikit-image==0.22.0
